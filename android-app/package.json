{
  "name": "boto-android-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Capacitor wrapper that packages the Vibe-Trading SPA as an installable Android app.",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "build": "node build.mjs",
    "sync": "node build.mjs && cap sync android",
    "open": "cap open android",
    "apk": "node build.mjs && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
